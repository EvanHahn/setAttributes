{
  "name": "setattributes",
  "version": "0.1.0",
  "description": "set and delete multiple attributes in a DOM element",
  "main": "setattributes.js",
  "scripts": {
    "pretest": "npm run lint:eslint && npm run lint:prettier",
    "lint:eslint": "eslint .",
    "lint:prettier": "prettier --check \"**/*.{md,js,json}\"",
    "format": "prettier --write \"**/*.{md,js,json}\"",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/EvanHahn/setAttributes.git"
  },
  "bugs": "https://github.com/EvanHahn/setAttributes/issues",
  "keywords": [
    "setattribute",
    "dom",
    "removeattribute"
  ],
  "license": "Unlicense",
  "devDependencies": {
    "eslint": "^7.16.0",
    "mocha": "^8.2.1",
    "prettier": "^2.2.1"
  }
}
